// ignore_for_file: camel_case_types

import 'package:flutter/material.dart';

class entrou extends StatelessWidget {
  entrou({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('voce entrou')),
    );
  }
}
